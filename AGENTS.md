# 🤖 AGENTS.md — Инструкция для AI-агентов по проекту Jouët Sheriff 600

Этот документ предназначен для любого AI-агента (Antigravity, Cursor, Claude Code, Copilot, ChatGPT), который продолжит работу над этим репозиторием.

---

## 1. Контекст и цель проекта

- **Проект:** Высокоточная параметрическая 3D-модель классической парусно-моторной яхты **Jouët Sheriff 600** (конструктор — Philippe Harlé, верфь — Yachting France, Франция, 1969).
- **Целевой 3D-принтер пользователя:** **Flashforge Adventurer 3** (рабочая зона 150 × 150 × 150 мм, диаметр сопла 0.4 мм).
- **Масштаб:** **1:45** (длина модели LOA = 133.3 мм, ширина Bmax = 52.3 мм, высота ватерлинейной части 26.8 мм). Вся модель гарантированно помещается на стол принтера. Также экспортируются файлы 1:1 (LOA = 6000 мм).
- **Текущее состояние (V4):**
  - Подводная часть, форма корпуса, транец, чугунный фальшкиль и навесной руль согласованы с чертежами и **не менялись с V3** (проверено: `Split_Keel` V4 совпадает с V3 с точностью 0.0000 мм).
  - **Пользователь уже может печатать нижнюю часть с килем (`Jouet_Sheriff_Split_Keel_1to45.stl`).**
  - Палуба, рубка и кокпит **переделаны с нуля по фотографиям реальной лодки** (`real_photos/`) без замеров — пропорции оценены по фото и чертежу общего расположения. Следующий шаг — уточнение размеров по замерам пользователя и ответы на открытые вопросы из раздела 6.

---

## 2. Архитектура кода и генератора

Основной скрипт генерации: **`generate_sheriff_models.py`**. Все размеры палубы вынесены в блок констант в начале файла (`X_BULKHEAD`, `SLOPE_ANGLE`, `SEAT_Z`, `LOCKER_SIDE` и т.д.) — правки начинайте с них.

### Слой 1. Корпус — топология «Шпангоутных колец» (Ring Topology)
Функция `get_sheriff_ring(x)` строит замкнутый полигон при каждой координате $X$:
Киль $\to$ Правый борт (суперэллипс $p, q$) $\to$ Правый привальный брус (валик) $\to$ **плоская палуба с погибью 35 мм** $\to$ Левый привальный брус $\to$ Левый борт $\to$ Киль.
121 кольцо соединяются в чулок и закрываются заглушками транца ($X=0$) и форштевня ($X=6000$). Часть кольца ниже привального бруса **идентична V3** — её нельзя менять, иначе разойдётся стыковка с напечатанным килем.

### Слой 2. Надстройка — плоские тела и CSG через `manifold3d`
Рубка и кокпит **не рисуются внутри кольца** (так в V3 получались волны и косые грани), а собираются из отдельных тел:
- `wall_geometry(x, d)` / `cabin_section(x, d)` / `cabin_body(d)` — лофт сечений рубки вдоль борта. Верхняя поверхность каждого сечения считается на сетке по $Y$ как `min(крыша с погибью, smooth_min(наклонная стенка 20°, передний скат))`. Крыша линейна по $X$ и заканчивается полуэллипсом (`ROOF_NOSE_L` вперёд по ДП, полуширина крыши поперёк); передний скат падает вперёд под `SLOPE_ANGLE` от этой дуги: `z = крыша − tan(α)·(x − x_перегиба(y))`. Так перегиб на крыше и линия подножия — дуги, а угол скат/стенка сглажен на `NOSE_CORNER_BLEND`. **Скруглена только передняя часть**; кормовые углы острые, кормовая стенка — плоскость (пользователь просил именно так; попытка скруглить кормовые углы дала ломаные грани). Параметр `d` даёт смещённую наружу/внутрь копию тела — так делаются рамки окон (`^ cabin_body(+12)`), утопленные стёкла (`- cabin_body(-8)`) и приподнятый люк (`^ cabin_body(+15)`).
- `groove_sections(sign)` — проходы на крыше как **канавки**: плоский вырез на `WALKWAY_DEPTH` ниже линии края крыши, шириной `WALKWAY_W`, от кормовой кромки (лофт начинается за плоскостью переборки, срез уже сделан) и дальше вперёд сквозь нос: там скат уходит ниже дна канавки, поэтому канавка заканчивается ровно на дуге перегиба — «до самого конца крыши». Снаружи остаётся ободок `WALKWAY_RIM` на уровне крыши. Никаких добавленных рельсов/бортиков — они выглядели отдельными плашками. Боковые окна **не** заходят на крышу; на крышу перегибается только передний люк (`HATCH_X0` на 50 мм позади перегиба на ДП).
- `foredeck_sections()` — **утопленный бак**: вырез из корпуса (до объединения с рубкой). Планширь `FOREDECK_RIM` = 50 мм с наклоном внутрь `FOREDECK_RIM_TILT`, скос `FOREDECK_SLOPE_W`, плоское дно ниже палубы на `FOREDECK_RECESS_AFT` у рубки и `FOREDECK_RECESS_FWD` у носа; к носу сходится клином до `FOREDECK_END`. Нос рубки стоит на этом опущенном баке (тело рубки уходит на 180 мм ниже палубы).
- `rake_cut(x)` — полупространство за наклонённой кормовой переборкой (`BULKHEAD_RAKE` = 30°, параллельно рёбрам окна). `x_bulkhead_at(z)` — X плоскости на высоте z; `x_roof_aft` — где она выходит на крышу.
- `aft_block()` — **кормовая часть рубки** как лофт по высоте Z: на каждой высоте контур в плане = плоская задняя кромка при `x_bulkhead_at(z)` + четверти окружностей радиуса `R(z)`, линейно убывающего от `AFT_CORNER_R` (450) на уровне банок до ~0 у крыши, + прямые бока по линии стенки. `cab = (body − всё аft-ер x_aft_block_fwd) + (aft_block ^ body)`: блок задаёт контур в плане и наклон переборки, тело рубки — крышу и наклон боковых стенок. Так «плечо» банки — одна поверхность со стенкой рубки, заподлицо, а углы крыши остаются острыми и переборка плоской.
- Тело рубки (`cabin_section`) ниже палубы идёт **вертикально** от подножия стенки ($b(x) - 150$) до `CABIN_Z_BOT` = 360 — в одной линии с комингсом кокпита (`GUNWALE_W` = `CABIN_MARGIN` = 150).
- `cockpit_well` — лофт прямоугольников выше $Z=500$ шириной $b(x) - 149$ от транца до `x_aft_block_fwd` (сквозь зону скруглённых углов); рубка объединяется **после** выреза и сама заполняет свой контур, поэтому банки обходят скруглённые углы без уступа.
- `footwell` — трапециевидный колодец до пола $Z=230$ от транца ($X=80$) до `x_sill` — точки, где наклонная переборка выходит на уровень порожка `SILL_Z` = 350; `sill_cut` срезает материал за плоскостью переборки выше порожка, чтобы наклонная стенка продолжалась ниже банок.
- `door_pocket` — **вход в рубку**: `door_box ^ rake_cut(X_BULKHEAD + DOOR_RECESS/cos)` — карман глубиной 200 мм между плоскостью переборки и параллельной ей плоскостью, от `DOOR_Z0` = 375 (чуть выше порожка), вычитается из всей сборки после объединения с рубкой.
- Доска-упор (плашмя, 55 мм, почти во всю длину пола) на двух стойках, балка оттяжки, поперечная утка на середине бака — `mbox(...)`, объединяются последними.
Порядок сборки: `(hull - foredeck_well - cockpit_well - footwell - sill_cut) + keel + cabin` $\to$ `- door_pocket` $\to$ `+ fittings + cleat`. Порядок важен: вырезы кокпита делаются **до** объединения с рубкой.

### Рендер
`render_v3_clean.py` — собственный ортографический z-buffer-растеризатор (`zbuffer_render`). **Не используйте `Poly3DCollection` из mplot3d**: он сортирует полигоны по их центрам, и длинные узкие треугольники после CSG рисуются в неверном порядке — на крыше появляются ложные «рваные» веера, которых в геометрии нет (на это ушёл один лишний цикл правок).

### Разрезы и пазы
- `m_wl = m_full - cut_box` (срез по КВЛ $Z=0$), `m_bottom = m_full - top_box`.
- `m_keel_split = m_bottom - pin_pair(180)` — глубина паза в киле **180 мм**, как в V3.
- `m_deck_split = m_wl - pin_pair(130)` — в палубе паз **130 мм**: над кормовым штифтом ($X=1500$) лежит пол кокпита ($Z=230$), нужна стенка $\geq 100$ мм.

---

## 3. Критические правила для Агента

### ⚠️ ПРАВИЛО 1: Сохраняйте координаты стыковочных отверстий!
Пользователь печатает нижнюю часть (`Jouet_Sheriff_Split_Keel_1to45.stl`), в которой уже заложены два центровочных отверстия на плоскости ватерлинии ($Z=0$):
- **Носовое отверстие:** $X = 3800.0\text{ мм}$ ($X = 84.44\text{ мм}$ в масштабе 1:45), $Y = 0.0$, радиус $55\text{ мм}$ (диаметр $2.4\text{ мм}$ в 1:45).
- **Кормовое отверстие:** $X = 1500.0\text{ мм}$ ($X = 33.33\text{ мм}$ в масштабе 1:45), $Y = 0.0$, радиус $55\text{ мм}$ (диаметр $2.4\text{ мм}$ в 1:45).

**Любые доработки палубы ДОЛЖНЫ сохранять эти же ответные отверстия в `m_deck_split` при $Z=0$!** Иначе половинки не состыкуются штифтами. Пол кокпита (`FLOOR_Z`) не опускайте ниже `DECK_SOCKET_DEPTH + 100`.

### ⚠️ ПРАВИЛО 2: Прямые линии должны оставаться чистыми плоскостями
Требование пользователя: *"где должны быть прямые линии, обойтись без полигонов"*.
- Носовой скат, крыша рубки, переборка, банки, пол кокпита, транец и плоскость КВЛ должны быть **строго плоскими**.
- Новые элементы палубы добавляйте как отдельные тела через `manifold3d`, а не через деформацию кольца `get_sheriff_ring` — иначе появятся волны и косые грани между соседними кольцами (шаг 50 мм).
- В рендерах (`render_v3_clean.py`) используйте `edgecolor='none'` и шейдинг по нормалям.

### ⚠️ ПРАВИЛО 3: Только 100% Watertight Manifold
Любая сгенерированная модель должна выдавать:
```python
assert mesh.is_watertight == True
```
Слайсер FlashPrint выдаст ошибку или испортит слои, если в модели будут дыры или пересекающиеся внутренние грани.

### ⚠️ ПРАВИЛО 4: Проходы только на крыше
В Jouët Sheriff 600 **нет бортовых дорожек рядом с рубкой** (full-beam coachroof). Стенки рубки начинаются прямо от привального бруса. Ходят по крыше рубки.

---

## 4. Исходные данные и чертежи в репозитории

- **`real_photos/`** (🔥 **Главный ориентир для палубы, важнее чертежей 1969 года!**):
  - `photo_2026-09-10_15-19-13.jpg` — вид с левой раковины в кокпит: планширь, банки, пол, полукруглые плечи банок у переборки, открытая дверь сходного люка под тентом, стойка в полу. Жёлтая деталь на корме — **снятое перо руля**, а не банка. Белый ящик у переборки **не моделировать** — пользователь просил убрать рундук.
  - `photo_2026-09-10_15-19-28.jpg` — левый борт: окно-трапеция почти во всю стенку, сплошная скруглённая передняя стенка рубки, скат с акриловым люком, мачта, **утопленный бак** (планширь на уровне борта, палуба внутри ниже — это не бортик, а опущенная палуба), носовой релинг.
  - `photo_2026-09-10_15-19-32.jpg` — общий вид с левого борта от носа: пропорции бак / рубка / кокпит.
  - Уточнения пользователя: люк на скате — **тонированный акрил на петлях без рамки**; кокпит симметричен, банки и пол идут до транца, рундука нет; в полу кокпита стойки крепления **доски-упора для ног** и **поперечная балка**, за которую крепится оттяжка гика — обе детали нужно печатать; утка — маленькая, поперёк лодки, на середине бака.
- **`extracted_pdf_assets/Bateaux_oct69/`**:
  - `lines_plan_oriented.png` — Теоретический чертеж (Plan de formes: проекции «Корпус», «Полуширота», «Бок», шпангоуты 0..10).
  - `arrangement_plan_oriented.png` — План палубы и внутреннего обустройства.
  - `annotated_profile.png`, `annotated_half_breadth.png`, `annotated_body_plan.png`.
- **`downloaded_assets/assets/photos/`**:
  - Архивные фотографии других лодок серии (`ladesirade.jpg`, `port_RT_pont.jpg`, `port_RT_Arriere.jpg` и др.).
- **`chez_downloads/`**:
  - Оригинальные руководства пользователя и чертежи руля (`plan_safran_gunnar.wmf`, `manuel.PDF`).

---

## 5. Стандартный рабочий процесс доработки

Окружение: `python3 -m venv .venv && .venv/bin/pip install numpy scipy trimesh manifold3d matplotlib networkx rtree shapely pillow` (`.venv` в `.gitignore`).

Когда пользователь присылает фото и размеры:
1. Внимательно проанализируйте изменения (угол переборки, положение люка, размеры кокпита и т.д.).
2. Меняйте константы в начале `generate_sheriff_models.py`; новые элементы добавляйте как тела `manifold3d` в разделах 3–5 функции `build_jouet_sheriff_v4()`. Функцию `get_sheriff_ring()` и киль не трогайте.
3. Перед генерацией сохраните копию `stl_output/Jouet_Sheriff_Split_Keel_1to45.stl`, после — сравните с новой (объём и экстенты должны совпасть до 4 знака).
4. Запустите генератор: `.venv/bin/python generate_sheriff_models.py` — все модели должны быть `Watertight: True` (скрипт падает, если нет).
5. Запустите рендер: `.venv/bin/python render_v3_clean.py` и проверьте `stl_output/Jouet_Sheriff_3D_Renders.png`. Ракурсы 2 и 3 сделаны с левого борта, нос вправо — как на фотографиях пользователя, чтобы сравнивать напрямую.
6. Сделайте `git commit` и `git push`.

---

## 6. Открытые вопросы к пользователю (V4 сделана без замеров)

1. **Стойки / доска-упор / балка оттяжки.** Сейчас: доска 55×40 мм лежит плашмя на ДП от $X=200$ до $X=2100$ на высоте $Z=390..430$ (160 мм над полом) на двух стойках 60×60 при $X=300$ и $X=2000$; поперечная балка 60×60 при $X=1650$ от банки до банки на высоте $Z=420..480$. Уточнить высоту доски над полом и положение балки.
2. **Вход в рубку.** Проём 700 мм, от уровня банок (+20 мм) до 60 мм под крышей, карман 200 мм — размеры оценены по фото; уточнить ширину и высоту проёма.
3. **Носовой релинг и кронштейн подвесного мотора на транце** — не смоделированы (тонкие детали, при 1:45 ~0.5 мм). Нужно ли печатать?
4. **Утопленный бак** — глубина 70 мм у рубки и 160 мм у носа, планширь 50 мм с наклоном 10 мм, скос 150 мм — оценка по фото; уточнить. Глубина и ширина канавок на крыше (20 / 220 мм), ободок (60 мм), вынос дуги крыши (400 мм), угол ската (32°) и заход люка на крышу (50 мм) — тоже оценка.
5. **Основные размеры для уточнения:** длина бака от штевня до основания ската; длина плоской крыши; высота рубки над палубой; ширина и глубина пола кокпита; ширина банок; высота планширя над банкой; размеры окна и люка; радиус скругления кормовых углов у банок (450) и высота порожка (350).
